name: Pre-release

on:
  push:
    branches: [main]
    paths:
      - "src/manifest.json"
  workflow_dispatch:
    inputs:
      force:
        description: "Build even if versionName hasn't changed"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check if versionName changed
        id: version_check
        run: |
          CURRENT=$(jq -r '.versionName' src/manifest.json)
          echo "current=$CURRENT" >> "$GITHUB_OUTPUT"

          if git show HEAD^:src/manifest.json > /tmp/prev_manifest.json 2>/dev/null; then
            PREVIOUS=$(jq -r '.versionName' /tmp/prev_manifest.json)
          else
            PREVIOUS=""
          fi

          if [[ "$CURRENT" != "$PREVIOUS" ]]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Decide whether to build
        id: gate
        run: |
          if [[ "${{ steps.version_check.outputs.changed }}" == "true" || "${{ github.event.inputs.force }}" == "true" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Bun
        if: steps.gate.outputs.run == 'true'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install aiot-toolkit globally
        if: steps.gate.outputs.run == 'true'
        run: bun install -g aiot-toolkit

      - name: Add bun global bin to PATH
        if: steps.gate.outputs.run == 'true'
        run: echo "$HOME/.bun/bin" >> "$GITHUB_PATH"

      - name: Install project dependencies
        if: steps.gate.outputs.run == 'true'
        run: bun install --frozen-lockfile

      - name: Build
        if: steps.gate.outputs.run == 'true'
        run: aiot build

      - name: Verify build output exists
        if: steps.gate.outputs.run == 'true'
        run: |
          if ! ls dist/*.rpk > /dev/null 2>&1; then
            echo "::error::aiot build did not produce a .rpk file — check the build log above for the real error."
            exit 1
          fi

      - name: Create draft pre-release with asset
        if: steps.gate.outputs.run == 'true'
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ steps.version_check.outputs.current }}
          tag_name: v${{ steps.version_check.outputs.current }}
          prerelease: true
          draft: true
          files: dist/*.rpk
          generate_release_notes: true

      - name: Publish the release
        if: steps.gate.outputs.run == 'true'
        run: gh release edit v${{ steps.version_check.outputs.current }} --draft=false
        env:
          GH_TOKEN: ${{ github.token }}
