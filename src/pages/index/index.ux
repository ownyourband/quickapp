<template>
	<div class="page">
		<div class="page-header">
			<input class="btn btn-info" type="button" @click="navigateTo('info')" />
		</div>

		<scroll
			class="page-content"
			scroll-y="true"
			bounces="true"
			style="{{ data.content }}"
		>
			<text class="page-title">Own Your Band!</text>

			<div class="card" onclick="navigateTo('info')">
				<text>Connection status</text>
			</div>
			<div
				class="card"
				onclick="sendMessage(data.connection.instance, 'hello world')"
			>
				<text>Send hello</text>
			</div>

			<div class="card" onclick="navigateTo('info')">
				<div class="card-icon">
					<image class="icon-img" src="/common/icons/bug.png" />
				</div>
				<div class="card-content">
					<text>idk what this is</text>
				</div>
			</div>
		</scroll>
		<div class="page-footer">
			<input
				class="btn btn-blue btn-connect"
				type="button"
				@click="connectDevice"
			/>
		</div>
	</div>
</template>

<script>
import { navigateTo } from "../../common/js/routing";
import { sendMessage } from "../../common/js/connection";

export default {
	data: {
		cat: "purr",
	},
	navigateTo,
	sendMessage,

	onInit() {
		this.data = this.$app.$def.data;
		console.log(
			"index: this.data.connection.instance: ",
			this.data.connection.instance
		);
	},
};
</script>

<style>
/* For shared styles  */
@import url("../../common/css/main.css");

/* Page specific */
@import url("./style.css");
</style>
